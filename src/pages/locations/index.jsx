import { LocationsList } from "./components";
import { CTA } from "../../layout/components";

export const Locations = () => {
  return (
    <section>
      <LocationsList />
      <CTA />
    </section>
  );
};
